import { ExpectationResult } from '../../../types';

interface ExpectationStatusIconProps {
    className?: string;
    result: ExpectationResult;
}
declare const ExpectationStatusIcon: ({ className, result }: ExpectationStatusIconProps) => import("react/jsx-runtime").JSX.Element;
export default ExpectationStatusIcon;
